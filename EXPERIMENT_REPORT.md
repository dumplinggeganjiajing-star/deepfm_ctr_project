# DeepFM 双排推荐全量数据实验报告

更新时间：2026-09-18

## 1. 实验目的

本项目使用 DeepFM 对双排视频推荐页的曝光样本进行 CTR 预估和排序，并与现有 CatBoost 树模型进行离线比较。现阶段实验主要回答以下问题：

1. DeepFM version0 在与树模型相同业务窗口下的表现如何；
2. `userid`、`article_id`、`theme_id` 三个高基数 ID 同时进入 Embedding 是否有效；
3. 数据切分从固定日期改为 8/1/1 后，模型泛化表现如何变化；
4. 消除相邻集合日期重合后，新的可信基线是什么；
5. 文章和社区短期 CTR 趋势特征是否能够改善排序；
6. 用户最近点击间隔分桶及其与文章类型的交叉是否能够改善排序。

本报告只记录全量数据实验，不包含5%、10%等抽样实验以及早期单ID小样本消融。

## 2. 统一模型和评估口径

### 2.1 基础模型

当前稳定基线不包含以下高基数 ID Embedding：

```text
userid
article_id
theme_id
```

基础模型使用 Linear、FM 和 DNN 三部分，类别特征通过 Embedding 表示，连续特征分别进入 Linear 与 FM/DNN。基础类别 Embedding 参数量为972。

### 2.2 评估指标

所有完整评估包含：

```text
AUC
GAUC
LogLoss
NDCG@10
预测CTR均值与真实CTR
新用户GAUC
老用户GAUC
新文章GAUC
Embedding参数量
```

AUC衡量全局样本区分能力；GAUC和NDCG@10更接近同一用户候选集合内部的排序能力。双排推荐以排序为主要目标，因此不能只根据AUC或CTR校准判断模型优劣。

## 3. 实验阶段概览

| 阶段 | 数据切分 | 特征方案 | 主要目的 |
| --- | --- | --- | --- |
| A | 固定业务日期 | version0 | 与线上树模型正式比较 |
| B | 按样本行数精确8/1/1 | 无三ID与三ID | 定位三ID过拟合问题 |
| C | 近似8/1/1且完整自然日隔离 | F0、F1与F2 | 建立可信新基线并验证趋势、活跃度分桶和交叉特征 |

不同阶段的测试集日期跨度和样本构成不同，跨阶段指标只能用于描述任务难度变化，不能直接归因为模型特征的增减。只有同一阶段、相同测试样本上的实验才构成严格消融。

## 4. 固定业务日期实验

### 4.1 数据范围

```text
样本开始日期：2026-03-09
训练数据截止：2026-09-08
测试日期：2026-09-09
最终训练样本：约8,413,419
测试样本：33,812
batch_size：512
```

首先使用验证集早停选择最佳轮数，最佳 epoch 为6；随后将原训练集与验证集合并，固定训练6轮，形成最终模型。

### 4.2 调参阶段结果

| 指标 | 结果 |
| --- | ---: |
| AUC | 0.7630 |
| GAUC | 0.6441 |
| LogLoss | 0.1820 |
| NDCG@10 | 0.5703 |
| 预测CTR | 0.055521 |
| 真实CTR | 0.053502 |
| 新用户GAUC | 0.6475 |
| 老用户GAUC | 0.6414 |
| 新文章GAUC | 0.5578 |
| Embedding参数量 | 972 |

冷启动分群样本：

```text
新用户：16,966
老用户：16,846
新文章：14,216
```

### 4.3 最终模型结果

| 指标 | 结果 |
| --- | ---: |
| AUC | 0.7659 |
| GAUC | 0.6475 |
| LogLoss | 0.1813 |
| NDCG@10 | 0.5699 |
| 预测CTR | 0.057410 |
| 真实CTR | 0.053502 |
| 新用户GAUC | 0.6556 |
| 老用户GAUC | 0.6417 |
| 新文章GAUC | 0.5336 |
| Embedding参数量 | 972 |

冷启动分群样本：

```text
新用户：15,895
老用户：17,917
新文章：11,880
```

模型产物位于：

```text
/data/ganjiajing/deepfm_ctr_project/artifacts_compare_final_20260909/
```

### 4.4 与树模型比较

| 指标 | CatBoost树模型 | DeepFM version0 | DeepFM差值 |
| --- | ---: | ---: | ---: |
| AUC | 0.762981 | 0.7659 | 约+0.0029 |
| GAUC | 0.645793 | 0.6475 | 约+0.0017 |

DeepFM在该单次离线实验中略高于树模型，但差异不足0.003，且未做用户级置信区间或多随机种子验证，因此不能认定DeepFM显著优于树模型。新文章GAUC仅为0.5336，是该阶段最明显的短板。

## 5. 按样本行数精确8/1/1实验

### 5.1 数据切分

该阶段先按 `part_date` 排序，再按样本行号精确切分80%、10%、10%。

| 集合 | 样本数 | 日期范围 |
| --- | ---: | --- |
| Train | 6,757,784 | 2026-03-09至2026-08-07 |
| Val | 844,723 | 2026-08-07至2026-08-25 |
| Test | 844,724 | 2026-08-25至2026-09-09 |
| 合计 | 8,447,231 | 2026-03-09至2026-09-09 |

由于按行号截断，高流量边界日被拆分到相邻集合：

```text
train与val共享日期：2026-08-07
val与test共享日期：2026-08-25
```

### 5.2 三ID同时加入Embedding

新增特征：

```text
userid
article_id
theme_id
```

模型规模：

```text
Total params：7,128,671
模型权重约27.19 MB
每轮13,199个batch
单轮约17至18分钟
```

训练曲线：

| Epoch | Train AUC | Val AUC | Val LogLoss |
| ---: | ---: | ---: | ---: |
| 1 | 0.7661 | 0.6877 | 0.2021 |
| 2 | 0.8377 | 0.6224 | 0.2597 |
| 3 | 0.8794 | 0.5784 | 0.3591 |
| 4 | 0.9019 | 0.5502 | 0.4579 |

该实验没有继续形成可信的最终测试结果。训练AUC快速上升，而验证AUC和LogLoss持续恶化，说明三个高基数ID使模型快速记忆训练期实体及交互，产生严重过拟合。

### 5.3 无三ID控制组

移除三个ID，保持相同数据切分和训练条件。前四轮训练表现：

| Epoch | Train AUC | Val AUC | Val LogLoss |
| ---: | ---: | ---: | ---: |
| 1 | 0.7322 | 0.6953 | 0.1932 |
| 2 | 0.7444 | 0.6949 | 0.1934 |
| 3 | 0.7476 | 0.6987 | 0.1923 |
| 4 | 0.7493 | 0.6971 | 0.1935 |

测试结果：

| 指标 | 结果 |
| --- | ---: |
| AUC | 0.6970 |
| GAUC | 0.6128 |
| LogLoss | 0.2022 |
| NDCG@10 | 0.5494 |
| 预测CTR | 0.061472 |
| 真实CTR | 0.055835 |
| 新用户GAUC | 0.6151 |
| 老用户GAUC | 0.6075 |
| 新文章GAUC | 0.5993 |
| Embedding参数量 | 972 |

测试分群：

```text
测试集：844,724
新用户：655,247，约77.57%
老用户：189,477，约22.43%
新文章：391,359，约46.33%
```

预测CTR相对真实CTR高估约10.1%。无三ID模型的验证AUC稳定在约0.695至0.699，没有出现三ID方案的失控退化，因此三ID是严重过拟合的主要增量原因。

同时，无三ID模型也明显低于固定日期实验。这主要来自测试任务变化：训练截止时间提前、测试跨度扩大、冷启动比例提高以及时间分布漂移，而不是模型参数量变化。

## 6. 完整自然日隔离实验

### 6.1 切分方式改造

为消除相邻集合共享同一天的问题，切分逻辑改为：

1. 按自然日统计每日样本量；
2. 在日期之间选择最接近80%和90%累计样本量的两个边界；
3. 保证完整自然日不会跨集合。

代码约束为：

```python
train[DATE_COL].max() < val[DATE_COL].min()
val[DATE_COL].max() < test[DATE_COL].min()
```

每日流量不同，因此实际比例只能接近8/1/1，不保证精确相等。

新测试集统一为：

```text
测试样本：864,950
真实CTR：0.056367
新用户：672,719，约77.78%
老用户：192,231，约22.22%
新文章：382,787，约44.26%
```

相较旧的按行切分测试集，新测试集增加20,226条。因此旧8/1/1结果与完整自然日结果不能直接进行特征归因比较。

### 6.2 F1短期趋势特征实验

F1在无三ID版本上增加：

```text
article_click_rate_1d
theme_click_rate_1d
article_ctr_trend_1d_7d
theme_ctr_trend_1d_7d
article_global_vs_theme_ctr_lift_7d
```

其中最后一个字段表示文章全局近7日CTR相对于当前社区近7日CTR的倍数，并不代表按文章与社区组合统计的局部CTR。

F1测试结果：

| 指标 | 结果 |
| --- | ---: |
| AUC | 0.6946 |
| GAUC | 0.6083 |
| LogLoss | 0.2030 |
| NDCG@10 | 0.5453 |
| 预测CTR | 0.055731 |
| 真实CTR | 0.056367 |
| 新用户GAUC | 0.6139 |
| 老用户GAUC | 0.5956 |
| 新文章GAUC | 0.5840 |
| Embedding参数量 | 972 |

模型产物：

```text
/data/ganjiajing/deepfm_ctr_project/artifacts_f1_trend/
```

### 6.3 F0完整自然日新基线

删除所有F1新增字段，保留相同数据、自然日切分、模型结构与训练配置。

| 指标 | 结果 |
| --- | ---: |
| AUC | 0.6965 |
| GAUC | 0.6164 |
| LogLoss | 0.2046 |
| NDCG@10 | 0.5458 |
| 预测CTR | 0.066579 |
| 真实CTR | 0.056367 |
| 新用户GAUC | 0.6157 |
| 老用户GAUC | 0.6180 |
| 新文章GAUC | 0.5972 |
| Embedding参数量 | 972 |

模型产物：

```text
/data/ganjiajing/deepfm_ctr_project/artifacts_f0_disjoint_date/
```

该实验是当前最新、最可靠的控制基线。

## 7. F0与F1严格消融对比

F0和F1使用完全相同的864,950条测试样本及相同冷启动分群，因此指标变化可以归因于F1特征。

| 指标 | F0无趋势 | F1短期趋势 | F1变化 |
| --- | ---: | ---: | ---: |
| AUC | **0.6965** | 0.6946 | -0.0019 |
| GAUC | **0.6164** | 0.6083 | -0.0081 |
| LogLoss | 0.2046 | **0.2030** | -0.0016 |
| NDCG@10 | **0.5458** | 0.5453 | -0.0005 |
| 新用户GAUC | **0.6157** | 0.6139 | -0.0018 |
| 老用户GAUC | **0.6180** | 0.5956 | -0.0224 |
| 新文章GAUC | **0.5972** | 0.5840 | -0.0132 |
| 预测CTR | 0.066579 | **0.055731** | - |
| 真实CTR | 0.056367 | 0.056367 | - |

F1使LogLoss下降，并将CTR均值从相对高估约18.1%改善为相对低估约1.13%，说明概率校准明显改善。但是AUC、GAUC和NDCG@10均下降，其中老用户GAUC下降0.0224，新文章GAUC下降0.0132。

对于双排推荐排序，GAUC和NDCG优先于整体CTR均值校准，因此F1整体不保留。概率校准可在排序模型之外单独处理，不应以牺牲用户内部排序为代价。

## 8. F2用户最近点击间隔特征实验

F2继续使用与F0完全相同的完整自然日切分、864,950条测试样本、模型结构和训练配置。原始连续字段 `days_since_last_click` 在F0中已经入模，本轮验证其离散分桶以及与 `article_type` 显式交叉的增量价值。

分桶口径为：

```text
当天
1天
2至3天
4至7天
8至14天
15至30天
31天以上
缺失/非法
```

### 8.1 分桶与文章类型交叉

新增：

```text
days_since_last_click_bucket
days_since_last_click_article_type_cross
```

模型产物：

```text
/data/ganjiajing/deepfm_ctr_project/artifacts_f2_recency_cross/
```

| 指标 | F0基线 | F2分桶+交叉 | 变化 |
| --- | ---: | ---: | ---: |
| AUC | **0.6965** | 0.6961 | -0.0004 |
| GAUC | **0.6164** | 0.6129 | -0.0035 |
| LogLoss | 0.2046 | **0.2037** | -0.0009 |
| NDCG@10 | **0.5458** | 0.5407 | -0.0051 |
| 预测CTR | 0.066579 | 0.066432 | -0.000147 |
| 真实CTR | 0.056367 | 0.056367 | 0 |
| 新用户GAUC | 0.6157 | 0.6157 | 0 |
| 老用户GAUC | **0.6180** | 0.6066 | -0.0114 |
| 新文章GAUC | **0.5972** | 0.5966 | -0.0006 |
| Embedding参数量 | 972 | 1,215 | +243 |

该方案仅小幅改善LogLoss，AUC、GAUC和NDCG@10均下降，老用户GAUC下降0.0114。新增参数量很小，因此负收益不能归因为高维Embedding造成的容量膨胀。

### 8.2 仅保留点击间隔分桶

为判断负收益来自分桶本身还是显式交叉，删除交叉字段，仅保留：

```text
days_since_last_click_bucket
```

模型产物：

```text
/data/ganjiajing/deepfm_ctr_project/artifacts_f2_recency_bucket_only/
```

| 指标 | F0基线 | F2仅分桶 | 变化 |
| --- | ---: | ---: | ---: |
| AUC | **0.6965** | 0.6947 | -0.0018 |
| GAUC | **0.6164** | 0.6117 | -0.0047 |
| LogLoss | 0.2046 | **0.2035** | -0.0011 |
| NDCG@10 | **0.5458** | 0.5432 | -0.0026 |
| 预测CTR | 0.066579 | **0.059027** | 更接近真实值 |
| 真实CTR | 0.056367 | 0.056367 | 0 |
| 新用户GAUC | 0.6157 | 0.6159 | +0.0002 |
| 老用户GAUC | **0.6180** | 0.6021 | -0.0159 |
| 新文章GAUC | **0.5972** | 0.5953 | -0.0019 |
| Embedding参数量 | 972 | 1,017 | +45 |

仅分桶方案使预测CTR明显接近真实CTR，LogLoss也略有改善，但排序指标继续下降，尤其老用户GAUC下降0.0159。删除交叉后GAUC没有恢复，说明主要负收益来自点击间隔分桶本身，而非参数量或显式交叉。

`days_since_last_click` 已经以连续值进入F0。重复增加其离散表示，更容易帮助模型拟合不同活跃人群的整体点击率，却没有增加同一用户候选文章之间的差异信息。因此两个F2方案均不保留，正式基线仍为F0。

## 9. 不同切分方式下的结果

| 实验 | 测试范围特点 | 测试样本 | AUC | GAUC |
| --- | --- | ---: | ---: | ---: |
| 固定日期最终模型 | 单日测试，训练截止到测试前一天 | 33,812 | 0.7659 | 0.6475 |
| 精确行数8/1/1 F0 | 多日测试，边界日可重合 | 844,724 | 0.6970 | 0.6128 |
| 完整自然日8/1/1 F0 | 多日测试，日期严格隔离 | 864,950 | 0.6965 | 0.6164 |

从固定日期到比例切分后指标下降，不代表模型本身退化。三组实验的测试任务不同：单日测试变为多日未来测试，训练截止时间提前，新用户比例接近78%，新文章比例约44%，时间漂移和冷启动难度均显著提高。

## 10. 主要结论

### 10.1 三ID联合Embedding不适合当前方案

三个高基数ID使参数量上升到约713万，训练AUC迅速达到0.9019，而验证AUC下降到0.5502。该方案主要记忆历史实体及组合，无法泛化到未来时间窗口。

### 10.2 完整自然日切分更可信

新的切分方法避免train、val和test共享边界日期。虽然实际比例不是精确8/1/1，但时间关系更清晰，应作为后续实验的统一切分口径。

### 10.3 当前模型全局区分强于用户内部排序

最新F0基线：

```text
AUC：0.6965
GAUC：0.6164
差值：0.0801
```

这说明模型更擅长区分不同用户之间的整体点击概率，用户内部候选文章排序仍然偏弱。

### 10.4 全局短期热度没有改善排序

F1改善了LogLoss和预测CTR校准，但降低了GAUC，尤其伤害老用户和新文章排序。后续不应继续批量增加全局热度特征，而应优先增加可泛化的用户与内容匹配特征。

### 10.5 用户总体活跃度分桶没有改善排序

F2两阶段实验均改善了LogLoss或CTR校准，但降低了GAUC和NDCG，且对老用户影响最明显。用户总体活跃程度在同一用户的候选集合中通常保持不变，无法直接提供候选文章之间的排序差异；与 `article_type` 交叉也没有产生正向增量。

### 10.6 当前主要瓶颈是特征而不是模型容量

三ID实验已经证明盲目扩大参数量会导致过拟合。下一阶段应优先构建用户与主题、作者、内容类型和历史文章行为的交叉特征，而不是扩大DNN层数或Embedding维度。

## 11. 当前正式基线

后续所有全量特征实验统一使用以下基线：

```text
实验名称：F0-disjoint-date
数据规模：全量
切分方式：近似8/1/1，完整自然日严格隔离
三ID：不入模
F1趋势特征：不入模
F2点击间隔分桶及交叉：不入模
Embedding参数量：972

AUC：0.6965
GAUC：0.6164
LogLoss：0.2046
NDCG@10：0.5458
预测CTR：0.066579
真实CTR：0.056367
新用户GAUC：0.6157
老用户GAUC：0.6180
新文章GAUC：0.5972
测试样本：864,950
```

后续实验必须保持以下因素不变：

```text
数据文件及数据版本
start_date与end_date
完整自然日切分逻辑
随机种子
batch_size
epochs与patience
DeepFM模型结构
评估代码
测试样本数及冷启动分群样本数
```

每次只修改一组特征，确保指标变化能够准确归因。

## 12. 下一步实验建议

下一阶段优先验证能够在同一用户候选集合内部发生变化的用户与内容交叉信息，例如：

```text
has_user_theme_history
log1p(user_theme_click_cnt_7d)
log1p(user_theme_exposure_cnt_7d)
user_theme_ctr_smoothed_7d
user_theme_active_days_7d
user_theme_avg_duration_7d
```

由于上述用户—主题字段在现有样本中的缺失率可能很高，实施前必须先统计训练、验证和测试集覆盖率。建议只选择一个覆盖率达到可用水平的字段做最小实验，并显式区分缺失与真实零值。

不建议继续增加以下用户总体属性的重复表达：

```text
days_since_last_click的更多分桶
用户整体活跃度 × article_type
```

重点观察GAUC、NDCG@10、老用户GAUC和新文章GAUC，而不是只观察训练AUC和预测CTR均值。若需要改善F0的CTR高估问题，应将概率校准作为独立实验，不与排序特征改造混在同一轮实验中。
