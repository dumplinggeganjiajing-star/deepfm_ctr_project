# DeepFM 与树模型离线效果对比报告

## 1. 报告目的

本报告比较 DeepFM 与当前系统树模型在同一时间窗口下的 CTR 预测效果，重点考察全局区分能力和用户组内排序能力，并补充分析 DeepFM 的概率质量、Top-K 排序及冷启动表现。

## 2. 实验口径

两种模型使用以下日期边界：

```text
训练数据：2026-03-09 ～ 2026-09-08
测试数据：2026-09-09
训练样本：8,413,419
测试样本：33,812
```

DeepFM 根据前置验证确定训练6轮，随后使用完整训练窗口重新训练，再在9月9日测试集评估。训练过程未使用测试标签选择模型。

DeepFM 采用当前 version0 特征结构，`userid`、`article_id`、`theme_id` 不直接进入 Embedding；三者仅用于分组及冷启动评估。DeepFM 类别 Embedding 参数量为972。

## 3. 核心指标对比

| 指标 | 树模型 | DeepFM | DeepFM差值 | 结论 |
| --- | ---: | ---: | ---: | --- |
| AUC | 0.762981 | 0.7659 | 约 +0.0029 | DeepFM小幅领先 |
| GAUC | 0.645793 | 0.6475 | 约 +0.0017 | DeepFM小幅领先 |

换算为百分点：

```text
AUC 提升约 0.292 个百分点
GAUC提升约 0.171 个百分点
```

DeepFM在全局区分能力和用户组内排序能力上均取得小幅正向结果。AUC和GAUC的提升方向一致，说明收益不只是来自识别不同用户的基础点击率，用户内部候选排序也有改善。

不过，两项提升均小于0.003。在没有置信区间、重复随机种子或配对显著性检验的情况下，应表述为“离线小幅领先”，暂不能断言提升具有统计显著性。

## 4. DeepFM扩展指标

| 指标 | DeepFM结果 |
| --- | ---: |
| LogLoss | 0.1813 |
| NDCG@10 | 0.5699 |
| 预测CTR均值 | 0.057410 |
| 真实CTR | 0.053502 |
| 新用户GAUC | 0.6556 |
| 老用户GAUC | 0.6417 |
| 新文章GAUC | 0.5336 |
| Embedding参数量 | 972 |

树模型当前只提供了AUC和GAUC，缺少同口径的LogLoss、NDCG@10、预测均值及冷启动分群指标，因此本节只能用于描述 DeepFM 自身特征，不能判断 DeepFM 在这些指标上优于树模型。

### 4.1 概率校准

```text
预测CTR均值：5.7410%
真实CTR：    5.3502%
绝对偏差：  +0.3908个百分点
相对高估：  约7.3%
```

DeepFM的平均预测概率略高于真实点击率，存在轻度高估。如果预测值将直接参与线上分数融合、收益估计或阈值决策，建议后续进行分桶校准分析，必要时使用 Platt scaling 或 isotonic regression；如果只用于排序，该偏差的影响相对较小。

### 4.2 用户分群

| 用户分群 | 样本数 | 测试集占比 | GAUC |
| --- | ---: | ---: | ---: |
| 新用户 | 15,895 | 约47.0% | 0.6556 |
| 老用户 | 17,917 | 约53.0% | 0.6417 |

新老用户样本数合计33,812，与完整测试集一致。新用户GAUC比老用户高0.0139，说明当前模型在新用户样本上并未表现出明显的用户冷启动劣势。由于 `userid` 不直接入模，这一结果主要来自上下文、内容、位置和统计特征，而不是用户ID记忆。

老用户GAUC相对较低，可能意味着老用户兴趣更复杂、候选内容更相似，组内排序难度更高。后续可按用户活跃度、历史曝光量和历史点击量继续分层验证。

### 4.3 新文章表现

```text
新文章样本：11,880，占测试集约35.1%
新文章GAUC：0.5336
```

新文章GAUC仅略高于随机水平0.5，明显低于整体GAUC 0.6475，是当前模型最突出的薄弱点。说明现有文章历史统计特征对未见文章的覆盖不足，模型尚未充分利用能够跨文章泛化的内容信息。

优先改善方向包括：

- 文章文本、标题、类目和主题的可泛化表示；
- 作者、内容类型、时效性和质量特征；
- 不依赖文章历史曝光/点击的内容侧特征；
- 对新文章单独检查缺失率、默认值比例和特征漂移；
- 避免直接依赖高基数 `article_id`，此前消融实验已显示其会降低整体指标。

## 5. 训练表现

DeepFM固定训练6轮：

| Epoch | Loss | Train AUC |
| ---: | ---: | ---: |
| 1 | 0.2040 | 0.7256 |
| 2 | 0.2014 | 0.7373 |
| 3 | 0.2008 | 0.7399 |
| 4 | 0.2004 | 0.7417 |
| 5 | 0.2002 | 0.7428 |
| 6 | 0.2000 | 0.7438 |

训练过程平稳，Loss持续下降、AUC持续上升，没有出现数值发散。最终测试AUC高于训练日志中的在线累计AUC并不矛盾：训练AUC是在一个 epoch 内模型持续更新时累计得到，而测试AUC使用该轮结束后的固定模型一次性计算；测试日样本分布和排序难度也可能不同。

## 6. 结论

在本次同日期窗口、同样本规模的离线评估中：

1. DeepFM AUC为0.7659，较树模型0.762981高约0.0029。
2. DeepFM GAUC为0.6475，较树模型0.645793高约0.0017。
3. 两个核心指标均小幅领先，DeepFM具备进一步验证价值，但提升幅度较小，尚需显著性验证。
4. DeepFM预测CTR较真实CTR高约0.39个百分点，存在轻度高估。
5. 新用户表现良好，但新文章GAUC仅0.5336，是下一阶段最优先的优化方向。
6. 当前不能比较两种模型的LogLoss、NDCG和冷启动能力，因为树模型缺少同口径结果。

综合判断：DeepFM在此次离线核心指标上小幅优于树模型，可以进入更严格的复核阶段；现阶段不建议仅凭单次结果直接替换线上模型。

## 7. 后续建议

1. 导出两种模型在同一33,812条测试样本上的逐样本预测，通过 `sample_id` 严格对齐。
2. 使用用户级 bootstrap 或配对检验，为 AUC、GAUC差值计算95%置信区间。
3. 使用多个随机种子复训 DeepFM，报告均值和标准差。
4. 用完全相同的评估代码补算树模型 LogLoss、NDCG@10及新老用户/文章指标。
5. 优先诊断新文章特征覆盖和缺失情况，再设计内容侧泛化特征。
6. 若离线收益稳定，再开展小流量线上A/B测试，观察CTR及业务约束指标。

## 8. DeepFM产物

```text
/data/ganjiajing/deepfm_ctr_project/artifacts_compare_final_20260909/deepfm_20260908.keras
/data/ganjiajing/deepfm_ctr_project/artifacts_compare_final_20260909/preprocessor_20260908.json
/data/ganjiajing/deepfm_ctr_project/artifacts_compare_final_20260909/training_metadata_final_20260909.json
```
